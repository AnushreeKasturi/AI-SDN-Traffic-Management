os-ken>=4.2
eventlet>=0.40
networkx>=3.0
scikit-learn>=1.3
pandas>=2.0
numpy>=1.24

# System packages (install via apt, not pip):
#   sudo apt-get install -y mininet openvswitch-switch openvswitch-testcontroller \
#                            iproute2 iputils-ping iperf
